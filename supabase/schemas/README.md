# Supabase Schemas

These SQL files document the intended database boundaries for the platform and predefined tracker modules.

They are not a dynamic schema builder. Each tracker should introduce its own explicit tables, constraints, indexes, policies, and migrations when implementation begins.
